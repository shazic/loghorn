jQuery(document).ready(function($){
  //$('.loghorn-color-cp').wpColorPicker();		// This would invoke the original wp color picker
	$('.loghorn-color-cp').alphaColorPicker();	// This invokes Braad Martin's color picker with alpha setings on!
});